# AGENTS instructions

- If the user says "use brain_tool" or requests brain_tool actions, run `brain_tool use --task "..."`.
- Use `brain_tool env` for diagnostics; use `--brain_home` if the repo is not the default.
- Only run `brain_tool fix_repo` with explicit approval.
