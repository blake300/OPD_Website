# GitHub Copilot custom instructions

- When the user says "use brain_tool", run `brain_tool use --task "..."` in the terminal.
- Use `brain_tool env` for diagnostics.
- Only run `brain_tool fix_repo` with explicit approval.
